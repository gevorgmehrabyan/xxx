declare module 'pdf-generator-xxxxx-3' {
    // You can define specific types here, for example:
    // export function createPDFDocument(data: any, formType: string): any;

    // Or you can declare everything as `any` if you don't have specific types:
    const pdfGenerator: any;
    export default pdfGenerator;
}
